---


---


I want to use NATS.io to setup a producer and consumer to handle my notificaiton system

one api endpoint: to send out a msg, this msg goes into jetstream via producer
and there's another consumer to receive that message and process it (for now, let's just put this message into minio)


includeing minio and NATS.io docker file, so I can locally set them up



您希望通知訊息的格式為何（例如 JSON 包含哪些欄位）？

訊息的格式包括
title
name
message


另外，在處理接收到的訊息並存入 MinIO 時，對於檔名或儲存路徑是否有特定的命名規則？
msg_{year}_{date}_{hourmin}_sec(2 digit)
example: msg_2021_1203_0823_23 